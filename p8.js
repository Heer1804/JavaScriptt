class Stack {
  constructor() {
    this.item = [];
  }

  isEmpty() {
    return this.item.length === 0;
  }

  push(element) {
    this.item.push(element);
  }

  pop(element){
    if(){
        
    }
  }
}